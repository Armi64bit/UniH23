import { BlocDirDirective } from './bloc-dir.directive';

describe('BlocDirDirective', () => {
  it('should create an instance', () => {
    const directive = new BlocDirDirective();
    expect(directive).toBeTruthy();
  });
});
