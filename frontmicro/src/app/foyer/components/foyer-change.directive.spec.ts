import { FoyerChangeDirective } from './foyer-change.directive';

describe('FoyerChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new FoyerChangeDirective();
    expect(directive).toBeTruthy();
  });
});
